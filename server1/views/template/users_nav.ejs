<header class="masthead mb-auto">
    <div class="inner">
        <nav class="sub_nav">
            <form method="GET">
                <button class=<%= selected == "default" ? "sub_nav_button_selected" : "sub_nav_button" %> type="submit" formaction="/users">All Users</button>
                <button class=<%= selected == "add" ? "sub_nav_button_selected" : "sub_nav_button" %> type="submit" formaction="/users/adduser">Add User</button>
              </form>
        </nav>
    </div>
</header>